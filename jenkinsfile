pipeline {
    agent any

    parameters {
        string(name: 'USERNAME', defaultValue: 'Likitha', description: 'Enter your name')
    }

    stages {
        stage('Hello') {
            steps {
                echo "Hello, ${params.USERNAME}!"
            }
        }
    }
}
